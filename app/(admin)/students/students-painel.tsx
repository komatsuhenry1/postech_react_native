
import { Screen } from "@/components/Screen";
import { Text } from "react-native";

export default function StudentsPainelScreen() {
    return (
        <Screen>
            <Text>Students Painel</Text>
        </Screen>
    );
}