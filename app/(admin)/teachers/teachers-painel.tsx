
import { Screen } from "@/components/Screen";
import { Text } from "react-native";

export default function TeachersPainelScreen() {
    return (
        <Screen>
            <Text>Teachers Painel</Text>
        </Screen>
    );
}